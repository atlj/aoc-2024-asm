procs:
  make-and-run: 
    shell: make && build/main
    autostart: false
  debug: 
    shell: make && lldb build/main
    autostart: false
  clean-make: 
    shell: make clean && make
    autostart: false
  clean: 
    shell: make clean
    autostart: false
