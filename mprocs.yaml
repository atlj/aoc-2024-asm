procs:
  make-and-run: 
    shell: make && build/hello
    autostart: false
  debug: 
    shell: make && lldb build/hello
    autostart: false
  clean-make: 
    shell: make clean && make
    autostart: false
  clean: 
    shell: make clean
    autostart: false
